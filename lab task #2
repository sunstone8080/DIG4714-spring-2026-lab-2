//person will divide to find the person who said 100
// order tracks if the next person is the person in front or behind

int person = 1;
int order = 1;


for int i = 1; i != 100; i++
//check for divisible by 7 and reverse order
  if(i % 7 == 0 )
    order = order * -1;
//check for divisible by 11 and then apply changes to the person
  if(i % 11 == 0)
   person = person + (order*2);
  else 
    person = person + order;

return person%10
//assuming the language drops the remainder instead of rounding it to the nearest whole
